# macOS
.DS_Store

# Local / personal — not shared (skills and .claude/settings.json ARE committed)
.claude/settings.local.json
CLAUDE.local.md
*.log

# Dependencies (none currently — static site)
node_modules/

# Secrets & Vercel local state
.env
.env.*
!.env.example
.vercel
